// ----- This is from 1.6 Lesson - Walk Thru -----
// Uppercase function for TEST
//const uppercase = (str) => str.toUpperCase();
// Lowercase function for TEST
//const lowercase = (str) => str.toLowerCase();
//exporting the module
//module.exports = {
    //uppercase,
    //lowercase
//}

// ---- This is updated information for 1.7 Assignment ----

// Testing Addition of two numbers
const addition = (str) => (str); 
// Testing Subtraction of two numbers
const subtract = (str) => (str);
// Testing Dividing of two number
const divide = (str) => (str);
// Testing Multiplication of two numbers
const multiply = (str) => (str);
// Testing Finding the Square Root of a number
const findSqrt = (str) => (str);
// Testing the finding of a max between two numbers
const findMax = (str) => (str);

// Exporting modules to 
module.exports = {
    addition,
    subtract,
    divide,
    multiply,
    findSqrt,
    findMax,
}